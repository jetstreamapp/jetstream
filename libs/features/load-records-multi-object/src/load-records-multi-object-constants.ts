/**
 * Floor for every data grid on the page. Grid height is derived from where the grid starts on the page, so a
 * long list of validation errors above it would otherwise shrink it until it cannot be read. Below this the
 * page scrolls instead.
 */
export const MIN_GRID_HEIGHT = '50vh';
