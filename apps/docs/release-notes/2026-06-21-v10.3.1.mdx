---
slug: v10.3.1
title: 10.3.1 - Data table polish
date: '2026-06-21'
tags: [web, extension]
versions:
  web: 10.3.1
  extension: 3.3.0
summary: A couple of small but welcome usability fixes for the rebuilt data tables.
highlights:
  - title: Steadier tooltips and column resizing
    description: Tooltips wait a touch longer before opening, and resizing a column no longer accidentally starts dragging it.
    docLink: /query/results
---

{/* truncate */}

## What's new

### Data table usability polish

Two small fixes make the rebuilt tables feel better to use:

- Tooltips now wait 500ms before opening (up from 300ms), so they're less likely to pop up while you're
  just moving the mouse across the table.
- Resizing a column no longer accidentally triggers a column drag, so adjusting widths stays separate from
  reordering.
